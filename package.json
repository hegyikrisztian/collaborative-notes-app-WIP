{
  "name": "collaborative-notes-app",
  "version": "1.0.0",
  "private": true,
  "description": "A collaborative notes application",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint"
  },
  "workspaces": [
    "client",
    "ws-server"
  ]
}
